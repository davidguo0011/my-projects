
// Define the interface for the draft listing state
export interface DraftListingState {
    draftListing: any | null; // Replace 'any' with a more specific type if available
    listingID: string | null;
}